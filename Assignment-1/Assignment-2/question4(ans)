1. Whimsical Toys Inc (WTI) needs to record the names of all its employees. Every month, an employee will be chosen at random from these records to receive a free toy.
   ans:- List => According to the task given,we can use a List for storing all the records of the employees and in this we can choose a random employee and pick a employee from the list for receiving a free toy.i.e (0 to size()-1)

2. WTI has decided that each new product will be named after an employee but only first names will be used, and each name will be used only once. Prepare a list of unique first names.
   ans:- Set => According to the task given,we can use a Set. As in set all the elements are unique ,not repetition of the elements will be there.

3. WTI decides that it only wants to use the most popular names for its toys. Count up the number of employees who have each first name.
   ans:- Map => According to the task given,we can use a Map,which is actually a storage for key-value pair where the keys are first names, and each value is a count of the number of employees with that first name.

4. WTI acquires season tickets for the local lacrosse team, to be shared by employees. Create a waiting list for this popular sport
   ans:- Queue =>According to the task given, we can use a Queue. In this we can make a call to add() function to add employees to the waiting list, and remove() function to remove them.
